// String, number, undefined, boolean, null, symbol

const nome1 = 'Kawhan'; // String
const nome2 = "Kawhan"; // String
const nome3 = `Kawhan`; // String

const num1 = 10; // Number
const num2 = 10.52 // Number

let nomeAluno; // undefined -> não aponta para lugar nenhum na memória
let sobrenomeAluno = null; // Nulo -> não aponta para lugar nenhum na memória

const desligado = false; // boolean = true or false (lógico)
const ligado = true; // boolean

// const a = [1, 2];
// const b = a;

// console.log(a, b);

// b.push(3);
// console.log(a, b);